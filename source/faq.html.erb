---
title: FAQ|Daggy
description: Most frequently asked questions about Daggy
tags: daggy, logstash, ansible, faq, wireshark, serverless, logging, data aggregation, logs, multiplexer, ssh
---
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
<% data.faq.each_with_index do |faq, index| %>
  {
    "@type": "Question",
    "name": "<%= faq.name %>",
    "acceptedAnswer": {
      "@type": "Answer",
      "text": "<%= faq.answer %>"
    }
  }<%= ',' if index != (data.faq.size - 1) %>
<% end %>
  ]
}
</script>


<div class="container-fluid text-lef" id="features">
  <div class="container">
    <div class="row text-center pb-5">
      <h1 class="cell-12 text-bold"><%= current_page.data.description %></h1>
    </div>
    <div data-role="accordion" data-one-frame="true" data-show-active="true">
    <% data.faq.each do |faq| %>
        <div class="pb-10" id="<%= faq.id %>">
          <a class="pb-10 h2 fg-accent text-bold" href="#<%= faq.id %>"><span class="text-bold">Q:</span> <%= faq.name %></a>
          <div class="p-1 text-bold">
            <span class="text-bold">A:</span> <%= faq.answer %>
          </div>
        </div>
    <% end %>
    </div>
  </div>
</div>